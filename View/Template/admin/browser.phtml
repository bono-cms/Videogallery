<?php

use Krystal\Form\Element;

?>

<div class="btn-toolbar">
    <div class="btn-group">
      <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
        <span class="glyphicon glyphicon-list"></span> <?php $this->show('Menu'); ?><span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
         <li><a href="#"><i class="glyphicon glyphicon-plus"></i> <?php $this->show('Add new file'); ?></a></li>
         <li><a href="#"><i class="glyphicon glyphicon-plus"></i> <?php $this->show('Add category'); ?></a></li>
         <li class="divider"></li>
         <li><a href="#" data-button="save-changes"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save changes'); ?></a></li>
         <li><a href="#" data-button="remove-selected"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>
      </ul>
    </div>
</div>

<?php $this->loadBlock('items-per-page', array('caption' => 'Video-gallery')); ?>

<form>
    <div class="table-responsive">
        <table class="table table-striped table-hover table-bordered table-condensed">
            <thead>
                <tr>
                    <th>
                        <?php if (!empty($files)): ?>
                        <input type="checkbox" />
                        <?php endif; ?>
                    </th>
                    <th><?php $this->show('#'); ?></th>
                    <th><?php $this->show('Title'); ?></th>
                    <th><?php $this->show('Default'); ?></th>
                    <th><?php $this->show('SEO Enabled'); ?></th>
                    <th><?php $this->show('Actions'); ?></th>
                </tr>
            </thead>
            <?php if (!empty($files)): ?>
            
            <tbody>
                <?php foreach ($files as $file): ?>
                <tr>
                    <td>
                        <input type="checkbox" name="toDelete[<?php echo $file->getId(); ?>]" />
                    </td>
                    <td><?php echo $file->getId(); ?></td>
                    <td><?php echo $file->getTitle(); ?></td>
                    <td>
                        <?php echo Element::radio('default', $file->getId(), $file->getDefault()); ?>
                    </td>
                    <td>
                        <?php echo Element::checkbox(sprintf('seo[%s]', $file->getId()), $file->getSeo()); ?>
                    </td>
                    <td>
                        <a class="view" href="<?php echo $file->getUrl(); ?>"></a>
                        <a class="edit" href="<?php echo $this->url('Videogallery:Admin:File:Edit@indexAction', $file->getId()); ?>"></a>
                        <a class="delete" href="#" data-id="<?php echo $file->getId(); ?>"></a>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
            <?php endif; ?>
            
        </table>
    </div>
</form>

<?php $this->loadBlock('paginator'); ?>
